---
"@looopy-ai/core": patch
---

Infer resume invocation type when `type` is omitted and the invocation payload contains `credentials` or `inputs`, while preserving explicit type precedence and existing parser validation rules.
